{
  "name": "bug-bounty-app",
  "version": "1.0.0",
  "description": "Bug Bounty Reconnaissance Platform",
  "scripts": {
    "install:backend": "cd backend && npm install",
    "postinstall": "npm run install:backend",
    "start": "cd backend && node server.js",
    "dev": "cd backend && npx nodemon server.js"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "license": "MIT"
}
